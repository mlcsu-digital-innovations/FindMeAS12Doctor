import { TableHeaderSortable } from './table-header-sortable.directive';

describe('TableHeaderSortable', () => {
  it('should create an instance', () => {
    const directive = new TableHeaderSortable();
    expect(directive).toBeTruthy();
  });
});
